pipeline {
    agent any

    stages {
        stage('Copy git repo') {
            steps {
                sh "ls -lsa"
            }
        }
        stage('Display readme') {
            steps {
                //sh 'cat README.md'
            }
        }
    }
}